import 'package:flutter/cupertino.dart';

class PropertyCreate extends StatelessWidget {
  const PropertyCreate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
