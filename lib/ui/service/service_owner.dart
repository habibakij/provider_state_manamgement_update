import 'package:flutter/cupertino.dart';

class ServiceOwner extends StatelessWidget {
  const ServiceOwner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
